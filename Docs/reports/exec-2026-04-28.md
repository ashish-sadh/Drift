# Drift Daily Briefing — April 28, 2026

## Headline
Two features shipped today: smart meal reminders that learn from user timing patterns, and an optional cloud AI backend with BYOK support.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 185 (shipped Apr 28, 2026) |
| Test Suite | ~2,974 tests (2061 iOS + 913 DriftCore) |
| AI Capabilities | 20 tools |
| Food Database | 2,511 foods |

## What Shipped This Period
- **Smart meal reminders (#385)** — App now learns when you typically eat and sends contextual nudges at those times; no configuration needed
- **Cloud AI backend with BYOK (#515)** — Users can opt-in to cloud processing by supplying their own API key; local-first remains the default; a UI toggle switches between CPU and cloud modes
- **Coverage gate fix** — Singleton lock prevents parallel test runs from deadlocking the CI loop
- **Watchdog hardening** — Fixed a latent `pipefail+grep` path that caused silent crash under certain shell environments

## What's Working Well
- AI pipeline reliability steady at 96% (48/50 gold-set queries) across log_food, log_weight, mark_supplement, food_info, edit_meal
- DriftCore extraction stable — pure logic builds on macOS in ~0.1s, keeping the inner loop fast
- TestFlight cadence healthy — build 185 shipped same day as feature merges

## Risks & Blockers
- `Docs/state.md` is stale (shows build 174; actual is 185) — needs a sweep after next feature
- 65 pending sprint tasks including 13 senior-level items; senior queue is the current bottleneck
- Metal GPU broken on A19 Pro devices — CPU inference only on latest hardware, ~2× slower TTFT

## Focus for Next Period
1. **Multi-intent splitting (#384)** — 'log lunch and update weight' should execute both tools in one turn; highest-impact AI quality gap
2. **Senior queue reduction** — 13 senior tasks pending; target 3–5 closures this session
3. **State.md refresh** — Sync build/test numbers so the snapshot stays trustworthy

## Cost
| | |
|---|---|
| Model | claude-sonnet-4-6 (human-shepherded session) |
| Sessions today | 0 autopilot (human-driven) |
| Est. cost today | $0.00 |
| Cost/cycle | N/A |

---
Comment on any line for strategic feedback. @ashish-sadh
