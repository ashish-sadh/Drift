# Drift Daily Briefing — 2026-04-21

## Headline
All four P0 bugs reported yesterday are closed and shipped; Photo Log (Cal AI) BYOK stack landed end-to-end; watchdog and preflight hooks hardened against two silent-failure classes.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 157 (shipped 2026-04-21) |
| Test Suite | 1,677+ tests, 38 test files |
| AI Capabilities | 20 tools (+ PhotoLog BYOK cloud vision) |
| Food Database | 2,511 foods |

## What Shipped This Period
- **Every reported P0 bug is closed.** #271 pizza search ranking (exact/prefix beats use_count), #275 passcode popup stacking 4× on beta settings, #278 Kind bar scanner misfiring ml (chocolate matched "cola" substring — classic false positive), #282 strawberry fiber rendering 0 instead of 1.5g (decimal-digit rule)
- **Photo Log (Cal AI) full stack landed** — BYOK key storage behind biometric keychain (#263), CloudVisionClient protocol + Anthropic impl (#264), PhotoLogService orchestration with caching (#265), Beta Settings UI + BYOK flow (#266), Capture + Review views beta-gated from Food tab (#267), AIToolAgent PhotoLogTool registration (#268). Design doc #236 → shipped product in ~24h
- **AI chat reliability infra** — Tool-call failure telemetry surfaced in daily session summary (#281), food_info now returns macro-goal progress with % for "am I hitting my protein goal" (#284), voice post-transcription repair for "metformin/creatine/whey" misrecognitions (#285), SmolLM↔Gemma 4 Tier 0/1 parity invariants locked by test (#286), ChatLatencyBenchmark single-item TTFT smoke runs by default to catch regressions (#287)
- **Food suggestion strip actually works** — taps open FoodLogSheet with adjustable servings instead of silent no-op (#270)
- **Mic permission request hardened** — explicit request before audio engine start (#272) — fixes real-device crash path
- **Exercise photo coverage 90% → 97%** — tiered fuzzy match (#273)

## What's Working Well
- P0 triage-to-ship cadence: #271/#275/#278/#282 all filed and fixed within the same working day. The substring-match root cause on #278 was hard (chocolate → "cola") and the session needed 4h of investigation, but it shipped with 4 regression tests
- Photo Log end-to-end in one sprint: design → keychain → service → UI → tool registration, six tight PRs, no partial surfaces
- Infrastructure debt paydown: preflight now fails-closed on P0 check (build 152 shipped with #271 open because of a silent `gh` flake — that pathway is sealed), watchdog matcher tightened so ordinary greps don't false-positive as archive attempts, commit-rate stall detector added so busy-but-unproductive sessions get killed at 3h instead of grinding forever, last-review-time no longer stamped at planner spawn (710-cycle silent review gap diagnosed today, fix shipped same commit)

## Risks & Blockers
- **Product review cadence was silently broken for days.** Last real product review merged Apr 18 (cycle 6046). Every planner spawn between then and now silently stamped last-review-time as "just done," so cmd_review_due never returned DUE. 710-cycle gap vs roadmap's 10-cycle cadence. Today's fix (abb7d09) unblocks — next planner fires at 09:56 PDT, should produce review-cycle-3910
- **Throughput bound on the P0 queue is Opus investigation time, not queue routing.** Routing is correct (P0 sprint-task first). But a single hard bug consumes 3-4h of a 5-task senior budget. #282 also had to be manually re-labeled from `approved` → `sprint-task` by an operator because CC's Approve flow doesn't add the routing label automatically
- **Design doc #274 (YouTube video curation for exercises) pending unwritten.** Stop-hook enforces in-review design PRs (unreplied comments) but doesn't enforce pending-unwritten docs. Senior sessions exit without writing them. Compliance gap, fix designed, not yet shipped

## Focus for Next Period
1. **First real product review at cycle 3910** — prove the watchdog fix works end-to-end by shipping the review PR on the next planner spawn. Ties off the silent-failure class
2. **CC Approve button adds sprint-task label atomically** — fix #282's class of "admin-approved but invisible to router." Two lines in `app.js`
3. **Design-doc compliance** — block senior Stop when pending docs exist and no design-doc PR was created this session. Closes the last systemic compliance hole

## Cost
| | |
|---|---|
| Model | claude-sonnet-4-6 (junior) / claude-opus-4-7 (senior/planning) |
| Sessions today | — (token-usage.sh not run yet) |
| Est. cost today | — |
| Cost/cycle | — |

## Decision Needed
None — all three follow-ups above are mechanical fixes well within routine scope.

---
Comment on any line for strategic feedback. @ashish-sadh
