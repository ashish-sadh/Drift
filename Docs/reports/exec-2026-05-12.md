# Drift Daily Briefing — 2026-05-12

## Headline
Medication tracking lands — design-574 schema + chat tools shipped today, so users can say "I'm on Ozempic 0.5mg weekly" or "took my metformin" in chat and the AI does the right thing.

## Product Snapshot
Drift is an AI-first health tracker — users type what they ate or did, AI handles logging, tracking, and insights. On-device, no cloud. Currently in closed beta on TestFlight.

| | |
|---|---|
| Beta Build | 243 (last published 242; build 243 awaits next 3h TestFlight window) |
| Test Suite | ~1253 iOS DriftTests + ~1366 macOS DriftCoreTests, ~160 LLM eval cases |
| AI Capabilities | 37 registered tools (24 core + 13 insight) — +2 from medication |
| Food Database | 5,420 foods (Indian-first curated, 6,000 ceiling enforced) |

## What Shipped This Period
- **Medication tracking, end-to-end (design-574 phases 1+2)**: `Medication` profile table + `MedicationLog` per-dose table landed (#751); chat tools `add_medication` and `log_medication` registered with idempotent profile creation by generic name OR brand, so "I'm on Wegovy" then "I'm on semaglutide" updates one row instead of duplicating (#752).
- **Chat-driven onboarding**: legacy `mark_supplement` path no longer wins for medication phrasing — "took my Ozempic" routes to `log_medication`, "started Ozempic 0.5mg weekly" routes to `add_medication`, with anti-keyword disambiguation that survives 17/17 routing assertions.
- **7-day Feedback activation banner** lands on dashboard (#759) — gentle nudge for week-1 users who haven't given friend feedback yet.
- **iCloud backup foundation** merged across #677/#678/#679 — BackupService, BGTaskScheduler nightly job, Settings restore picker + stale banner + first-launch prompt. (Entitlements reverted in #677 follow-up pending provisioning profile update — feature is wired but gated off.)
- **Design docs landed on main**: GLP-1 medication tracking (#574, implementation underway today) and lab report FM extraction (#665, next senior task in queue).

## What's Working Well
- The orphan-WIP recovery flow worked exactly as designed today: a prior session crashed mid-#752 with the partial patch staged, the sprint-service surfaced it on the next claim, applied cleanly, and the test feedback loop caught two real bugs in the orphan (brand-only re-add overwriting the generic name; "add metformin" routing to log_medication) before commit.
- DriftCore test suite stays fast — 1366 tests in 8.7s warm — so adding 15 medication-routing assertions added <0.2s, not a tier-budget concern.
- Senior queue continues to drain: 752 → 749 → ... with 12 SENIOR + 42 junior remaining. No P0 bugs on the queue.

## Risks & Blockers
- Build 243 has not yet been published to TestFlight — testflight-check.sh next window is the only gate; medication tracking won't reach beta users until that ships.
- Daily exec briefing hook continues to fire on every Bash call after a senior-task commit, even mid-session — same conflict flagged in the 2026-05-11 briefing. The fix is still owed: either an escape-hatch marker or moving the report into planning-mode exclusively.
- iCloud backup is wired but provisioning-profile-blocked. If the entitlement re-enable slips, beta users won't get backup before the next milestone.

## Focus for Next Period
1. **Lab report hybrid extraction (#749)**: SENIOR task already claimed — regex primary + FM gap-filler per design-665. This is the next prompt-refresh-grade unlock for users with messy lab PDFs.
2. **Medication tracking surface area (#753)**: Settings tab + weight-chart vertical marker at medication start date. JUNIOR-tagged, but it's the user-visible payoff of today's plumbing — should land in the same TestFlight cycle.
3. **Resolve the exec-briefing hook conflict** — third day this risk has been flagged. Worth a small infra ticket to either (a) gate the hook on planning-mode, or (b) add a session-scoped `report-skipped-today` marker.

## Cost
| | |
|---|---|
| Model | Opus 4.7 |
| Sessions today | 1 (current session, post-#751 land) |
| Est. cost today | n/a — token-usage.sh not run this cycle |
| Cost/cycle | n/a |

---
Comment on any line for strategic feedback. @ashish-sadh
