Control file for Drift self-improvement loop.
Check this file before every cycle.

Current status: REDIRECT
Direction:

NEVER STOP. Keep improving until human changes status to STOP.
Commit frequently. Don't publish TestFlight unless asked.
Check this file before every improvement cycle.

Read Docs/ai-architecture-plan.md FIRST — it defines the new architecture.
80% energy on Priorities 1-2. 20% on POC/research.

=== PRIORITY 1: REARCHITECT — LLM FOR INTENT, SWIFT FOR EXECUTION ===

Read: Docs/ai-architecture-plan.md

Current approach is wrong — hardcodes 30+ keyword checks in Swift for intent.
The 1.5B model IS good at understanding intent and extracting values.
What it's bad at is math, data recall, reasoning — Swift handles those.

New split:
- LLM classifies intent + extracts values from natural language
- Swift does computation, DB operations, UI actions

Key changes:
1. Update system prompt to teach model structured actions:
   [LOG_FOOD: name amount], [CREATE_WORKOUT: exercise 3x15@135], etc.
2. Remove most hardcoded keyword checks from sendMessage()
   Keep rule engine ONLY for exact matches (summary, calories left)
   Everything else → LLM classifies → Swift executes
3. LLM should ask clarifying questions when ambiguous, not guess

=== PRIORITY 2: CONVERSATIONAL WORKOUT BUILDER ===

Read: Docs/ai-architecture-plan.md — "Conversational Workout Builder" section

Three flows to implement:
A) Start from template: "let's do push day" → open ActiveWorkoutView
B) Build from conversation: "I did push ups 3x15, bench 3x10@135" → create workout
C) AI suggests: "what should I train?" → build plan from history + recovery

Implementation:
- New action [CREATE_WORKOUT: ...] in AIActionParser
- AIChatView presents ActiveWorkoutView via .sheet
- Multi-turn: AI asks "how many sets?" if not specified
- Save completed workout to DB

=== PRIORITY 3: RESPONSE QUALITY + EVAL HARNESS ===

Make every response clean and test everything:
- Strip markdown, preambles, disclaimers (already done)
- Catch low-quality responses → show fallback
- Run eval harness after every change
- Add workout intent test cases to harness
- Goal: 50+ test queries, all passing

=== PRIORITY 4: FOOD LOGGING POLISH ===

Food logging via chat should be FLAWLESS:
- All natural phrasing works
- Multi-food: "chicken and rice"
- Amounts: "a couple", "1/3", "half"
- Qualifiers: "2 slices of pizza"
- After action: show remaining calories
- Multi-turn: "What did you eat?" → "rice" → opens search

=== PRIORITY 5: POC / EXPLORATION (20%) — don't commit to main ===

These are research spikes. Use branches or /tmp. Don't merge unless works.

A) VISION MODEL SPIKE
   - Can llama.cpp b7310 load Qwen3-VL-2B + mmproj?
   - Write minimal test, document findings
   - DON'T commit to main

B) METAL GPU FIX SPIKE
   - Try GGML_METAL_USE_BF16=OFF — does it fix A19 Pro shader crash?
   - Try latest llama.cpp — maybe they fixed the shader bug
   - Document findings

C) STUDY CLAUDE CODE CLI PATTERNS
   - How does it handle tool calling, context management?
   - What makes the harness smart (not just the model)?
   - Save to Docs/ai-improvement-ideas.md

=== PRIORITY 6: BUG HUNTING ===

- Recent commits = where bugs hide
- Write tests for every new feature
- Edge cases: empty DB, no HealthKit, model not downloaded
- Full flow testing on simulator

=== ADDITIONAL DIRECTION: THINK BIGGER — INTELLIGENT PIPELINE ===

The priorities above are correct. But as you work on them, think about
the BIGGER PICTURE of what abstractions this system needs long-term.
Use this as a lens for making architectural decisions:

The #1 mission: REMOVE ALL FRICTION OF FORM FILLING IN THE APP.
Every manual data entry should be doable through natural conversation.

Think systematically about what infrastructure enables that:
- Memory layer (on-device KNN, embeddings, user preference recall)
- RAG-style retrieval (pull relevant past data before generating)
- Tool/skill registry (discrete tools LLM can invoke, each with schema)
- Intent identification (semantic, not keyword — the model IS good at this)
- Prompt routing (domain-specific prompts per intent, not one giant blob)
- Context orchestration (right data injected for each intent — context is king)
- Multi-turn state (conversation flows that span multiple messages)

Study Claude Code CLI for inspiration — how the HARNESS makes a small
model powerful through tool orchestration, context management, and
smart prompt routing. The model doesn't need to be smart. The system does.

Form-fill targets to keep in mind (all should work via chat eventually):
- Log food naturally ("chicken and rice", "200g paneer", "I had lunch")
- Log weight ("I weigh 75.2", "166 lbs")
- Nutrition queries ("top 3 protein items today", "calories left?")
- Food suggestions ("what should I eat? 600 cal left")
- Start workout from template ("let's do push day")
- Build workout from conversation ("bench 3x10@135, OHP 3x8@95")
- Smart workout suggestions ("what should I train today?")
- Training history ("what did I train last?")
- Build schedules ("make me a PPL split")

These are limitless questions. Build the pipeline so adding new
capabilities is just adding a new tool + prompt, not rewriting the system.

=== RESEARCH IDEAS (curate only, save to Docs/) ===

1) Voice input: speech-to-text → AI chat
2) Weekly AI summary notification
3) Fine-tuning health Q&A dataset
4) Apple Neural Engine via CoreML
5) Exercise form videos
6) On-device embeddings for food search
7) Photo food logging (after vision model POC)
8) Disk-based KNN for similar food/exercise retrieval
9) On-device RAG with local embeddings

=== HOW TO WORK ===

- Check .claude-stop status before every cycle
- Read Docs/ai-architecture-plan.md before starting
- Commit after each meaningful improvement
- POC work in branches or /tmp — never main
- Run tests + build after every change
- Push to git frequently

Status options:
- RUNNING: continue the loop
- STOP: finish current work, commit, halt
- PAUSE: commit and wait for instructions
- REDIRECT: read Direction field, apply changes, set back to RUNNING
